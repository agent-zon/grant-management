{
  "name": "grant-management-mtx-mtx",
  "dependencies": {
    "@cap-js/postgres": "^2.0.6",

    "@sap/cds": "^9",
    "@sap/cds-mtxs": "^3",
    "@sap/xssec": "^4",
    "express": "^4",
    "@cap-js/sqlite": "^2"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "cds-serve"
  },
  "cds": {
    "profiles": [
      "mtx-sidecar",
      "subscription-manager"
    ],
    "requires": {
      "destinations": true,
      "auth": {
        "kind": "ias"
      },
      "db": {
        "kind": "postgres",
        "deploy-format": "postgres"

      }
    }
  }
}
