{
  "name": "agent-grants",
  "version": "1.0.0",
  "description": "A simple CAP project.",
  "dependencies": {
    "@sap/xssec": "^4",
    "express": "^4",
    "sqlite3": "^5.1.7"
  },
  "devDependencies": {
    "@cap-js/sqlite": "^2",
    "@sap/cds-dk": "^9",
    "cap-js-mockdata": "^0.0.3"
  },
  "scripts": {
    "start": "cds-serve",
    "dev": "cds watch",
    "build": "cds build",
    "deploy": "cds deploy",
    "mockdata": "node scripts/generate-mockdata.js",
    "fix-mockdata": "./scripts/fix-mockdata.sh",
    "generate-data": "npm run fix-mockdata && npm run dev"
  },
  "private": true,
  "cds": {
    "requires": {
      "[production]": {
        "auth": "ias"
      },
      "destinations": true
    }
  }
}
